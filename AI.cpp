#include "AI.h"

AI::AI(const double & heightWeight, const double & linesWeight, const double & holesWeight,
	const double & bumpinessWeight)
	:heightWeight(heightWeight), linesWeight(linesWeight), holesWeight(holesWeight), bumpinessWeight(bumpinessWeight) {}
